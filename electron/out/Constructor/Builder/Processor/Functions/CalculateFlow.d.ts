import type { VoxelData } from "Meta/index";
import type { Processor } from "../Processor";
export declare function CalculateFlow(this: typeof Processor, voxelData: VoxelData, x: number, y: number, z: number, flowTemplate: number[]): void;
